# Anti-Entropy Bandits

**Visualization for Anti-Entropy Bandits**


## Getting Started

To generate the graph:

```
$ python -m bandit.generate fixtures/latency.csv
```

To draw the graph:

```
$ python -m bandit.draw fixtures/topology.json
```
